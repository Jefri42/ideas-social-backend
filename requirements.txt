gunicorn
psycopg2-binary
dj-database-url
whitenoise
Django==6.0.2
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2025.3
packaging==26.0
pillow==12.1.1
djangorestframework
djangorestframework-simplejwt
django-cors-headers
# mysqlclient solo se instala en Windows (desarrollo local con MySQL).
# En Linux no hay wheel: pip compila desde el sdist y necesita pkg-config y
# las cabeceras de MySQL, que no existen en el contenedor de Render, así que
# el build falla. Producción usa PostgreSQL vía DATABASE_URL (psycopg2-binary).
mysqlclient; sys_platform == "win32"
